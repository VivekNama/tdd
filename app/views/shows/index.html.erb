<h1>Shows List</h1>

<table>

  <thead>
    <tr>
      <th>Show Name</th>
      <th>Show Picture URL</th>
    </tr>
  </thead>

  <% @shows.each do |show| %>
    <tr>
      <td><%= show.name %></td>
      <td><%= show.picture %></td>
    </tr>
  <% end %>

</table>

<a href="/shows/new">New Show</a>
